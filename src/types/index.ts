// Tipos globales reutilizables
export type Project = {
  image: string;
  name: string;
  location: string;
  description: string;
};

export type Testimonial = {
  text: string;
  author: string;
};

export type Service = {
  icon: string;
  title: string;
  description: string;
};
